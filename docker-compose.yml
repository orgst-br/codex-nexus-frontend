services:
  dev:
    build: .
    volumes:
      - ./frontend:/app
      - node_modules:/app/node_modules
    ports:
      - "9000:9000"
    stdin_open: true
    tty: true
    environment:
      - NODE_ENV=development
    command: /bin/bash

volumes:
  node_modules:
